import styled from "@emotion/styled";
import { theme } from "../../../mui-config/theme";

const StyleFooter = styled.div`
  flex-grow: 0;
  flex-shrink: 0;
  flex-basis: 50px;
`;

const BoardFooter = () => {
  return <StyleFooter>Board Footer</StyleFooter>;
};
export default BoardFooter;
